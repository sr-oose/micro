spring:  
  application:
    name: rest-server
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: ${spring.application.name}
        #-${server.port}-${random.long}
      config:
        enabled: true
        format: yaml

---

spring:
  profiles: docker
  cloud:
    consul:
      host: consul-1
  
